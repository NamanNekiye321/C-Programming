#include <stdio.h>

int main() {
    
     int x = 5;
     int y = 4;

     printf("The value of 3*x / 2*y is %d\n",3*x / 2*y);


     //operator with same operator precedence will be evaluated from left to right as they have left-associative property.


    return 0;
}